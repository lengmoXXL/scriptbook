# Test dependencies for ScriptBook backend
pytest>=7.0.0
pytest-cov>=4.0.0  # Optional: for coverage reporting

# For testing Tornado applications (if needed)
# pytest-tornado>=1.0.0  # Uncomment if testing Tornado handlers

# For mocking (if needed)
# pytest-mock>=3.0.0  # Uncomment for better mocking support

# Note: Base dependencies (tornado, terminado) are in requirements.txt